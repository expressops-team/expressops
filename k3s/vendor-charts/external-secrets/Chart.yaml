apiVersion: v2
name: external-secrets-wrapper
description: Un chart wrapper para External Secrets Operator
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: external-secrets
    version: "0.16.2"  # Versión extraída del archivo gitops-argocd
    repository: "https://charts.external-secrets.io" 